#include <stdio.h>
#include <string.h>


int main() {
    char one[100];
    char two[100];
    scanf("%s",&one);
    scanf("%s",&two);
    printf("You entered:  %s and %s",one, two);
    
    return 0;
}